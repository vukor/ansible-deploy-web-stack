---
- name: Remove project user
  user: 
    name={{ prj_user }}
    remove=yes

- name: Remove project group
  group:
    name={{ prj_group }}
    remove=yes

- name: Remove project user's home
  file: >
    path=/home/{{ prj_user }}
    state=absent

